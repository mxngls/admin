/** @type {import('next').NextConfig} */
const nextConfig = {
    images: { domains: ["yedvegtvctkiowrixaru.supabase.co"] },
    reactStrictMode: true,
    swcMinify: true,
};

module.exports = nextConfig;
